<div class="flex flex-col items-center">
  <div class="absolute top-5 left-5 font-bold text-2xl">Book<span class="text-blue-500">Bridge</span></div>
  <%= form_with url: listings_index_path, method: :get, local: true do |form| %>
    <div class="flex space-x-4">
      <%= form.text_field :title, placeholder: "Title", class: "border p-2" %>
    </div>
  <% end %>
  <script></script>
</div>
